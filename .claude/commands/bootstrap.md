Bootstrap this Next.js + Supabase + Claude API project from scratch.

Use the `vercel:bootstrap` skill to scaffold the project, then:
1. Initialize Supabase project and apply schema
2. Configure environment variables (use `vercel:env` skill for Vercel env setup)
3. Wire up the Claude API with Vercel AI SDK
4. Confirm `npm run dev` starts cleanly before declaring done
