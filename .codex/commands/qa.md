Run browser QA on the running application.

1. Ensure the dev server is running (`npm run dev`)
2. Navigate each major flow: auth, core features, edge cases
3. Check browser console for errors — never suppress or ignore them
4. Fix small issues (wrong routes, missing props, null checks) inline
5. Report major issues (architecture, >3 files) before touching them
