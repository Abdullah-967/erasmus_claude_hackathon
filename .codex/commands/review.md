Review the current code changes for bugs, logic errors, and adherence to project conventions.

Check:
- TypeScript errors (no `any`, strict mode)
- Supabase RLS on all user-data tables
- No Claude API calls from client components
- Server Components used by default
- No inline `style=` props
